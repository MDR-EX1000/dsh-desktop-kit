{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "dsh-desktop-kit main window: the served loopback origin plus the kit_open_external and kit_set_zoom commands the injected client script calls.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*"
    ]
  },
  "permissions": [
    "core:default",
    "allow-kit-open-external",
    "allow-kit-set-zoom"
  ]
}
